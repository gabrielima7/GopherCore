version: "2"

run:
  timeout: 5m
  tests: true
  go: "1.26"

linters:
  enable:
    - errcheck
    - nilnil
    - nilerr
    - staticcheck
    - govet
    - unused
    - ineffassign
  settings:
    govet:
      enable:
        - nilness
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

issues:
  max-same-issues: 0

output:
  formats:
    text:
      path: stdout
      colors: true
